# ADMIN ROUTER INTEGRATION PATCH

## Modifications à faire dans backend/bot_webhook.py

### MODIFICATION 1: Import du router admin (ligne ~120)

Après la ligne:
```python
    analytics_router = None
```

Ajouter:
```python

# Admin Dashboard Router (Phase 1.5)
try:
    from backend.routes.admin import router as admin_router
    ADMIN_AVAILABLE = True
except ImportError:
    logger.warning("⚠️ Admin dashboard router not available")
    ADMIN_AVAILABLE = False
    admin_router = None
```

### MODIFICATION 2: Enregistrement du router (ligne ~150)

Après la ligne:
```python
    logger.warning("⚠️ Analytics router NOT registered")
```

Ajouter:
```python

# Include Admin Dashboard Router (Phase 1.5)
if ADMIN_AVAILABLE and admin_router:
    app.include_router(admin_router)
    logger.info("✅ Admin dashboard router registered at /admin/users")
else:
    logger.warning("⚠️ Admin dashboard router NOT registered")
```

## Vérification

Après les modifications, le fichier devrait contenir:

1. Section imports (ligne ~120):
```python
# Analytics System (Phase 1.5)
try:
    ...
except ImportError as e:
    ...
    analytics_router = None

# Admin Dashboard Router (Phase 1.5)  <-- NOUVEAU
try:
    from backend.routes.admin import router as admin_router
    ADMIN_AVAILABLE = True
except ImportError:
    logger.warning("⚠️ Admin dashboard router not available")
    ADMIN_AVAILABLE = False
    admin_router = None
```

2. Section routers (ligne ~150):
```python
# Include Analytics Router (Phase 1.5)
if ANALYTICS_AVAILABLE and analytics_router:
    app.include_router(analytics_router)
    logger.info("✅ Analytics router registered at /analytics")
else:
    logger.warning("⚠️ Analytics router NOT registered")

# Include Admin Dashboard Router (Phase 1.5)  <-- NOUVEAU
if ADMIN_AVAILABLE and admin_router:
    app.include_router(admin_router)
    logger.info("✅ Admin dashboard router registered at /admin/users")
else:
    logger.warning("⚠️ Admin dashboard router NOT registered")
```

## Test après modification

Après Railway redémarre, cherche dans les logs:
```
✅ Admin dashboard router registered at /admin/users
```

Si tu vois ça, le dashboard est actif!
